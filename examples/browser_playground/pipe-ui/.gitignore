# Build artifacts — regenerated by `npm install` / `npm run build`
node_modules/
dist/

# Track project config even though the root .gitignore ignores *.json
!package.json
!package-lock.json
!tsconfig.json
